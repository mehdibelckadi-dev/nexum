# Real API fixtures (too large and third-party IP)
nexum/tests/fixtures/real_*.json
nexum/tests/fixtures/real_*.yaml

# Sensitive data
data/findings_log.jsonl

# Generated files
*.pdf
web/static/reports/*.pdf
.env
__pycache__/
*.egg-info/
.venv/

# macOS metadata
.DS_Store
nexum/.DS_Store

# Scan input files in repo root (third-party specs, not committed fixtures)
/*.json
enterprise_rest_api_public.json

# Batch scan outputs — specs, PDFs, and logs never go to the repo
scans/
client-sdk-web/
batch_summary.txt
config.anthropic.spec.ts
client-sdk-web/
batch_summary.txt
config.anthropic.spec.ts

# Internal documentation — repo is public; these stay local only.
# CLAUDE.md stays at repo root (Claude Code loads it from there) but is untracked.
CLAUDE.md
private-docs/
